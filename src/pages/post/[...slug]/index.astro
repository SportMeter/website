---
import type { CollectionEntry } from "astro:content";
import PostLayout from "@/layouts/PostLayout.astro";
import { getCollectionStaticPaths } from "@/lib/collection-helpers";
import { defaultLocale } from "site.config";

export interface Props {
	data: CollectionEntry<"posts">;
}

export async function getStaticPaths() {
	return await getCollectionStaticPaths("posts", defaultLocale);
}

const { data: post } = Astro.props;
const { Content } = await post.render();
---

<PostLayout frontmatter={post}>
	<Content />
</PostLayout>
