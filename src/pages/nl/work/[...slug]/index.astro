---
import type { CollectionEntry } from "astro:content";
import WorkLayout from "@/layouts/WorkLayout.astro";
import { getPrefixedCollectionStaticPaths } from "@/lib/collection-helpers";

export interface Props {
	data: CollectionEntry<"works">;
}

export async function getStaticPaths() {
	return await getPrefixedCollectionStaticPaths("works", "nl");
}

const { data: work } = Astro.props;
const { Content } = await work.render();
---

<WorkLayout frontmatter={work}>
	<Content />
</WorkLayout>
